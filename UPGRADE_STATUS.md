# Upgrade Status

## Scope

Preserve existing data and UI. Paper trading only. Install LightGBM/CatBoost and use Chronos-2 for offline and shadow predictions before any decision promotion.

## Completed

- Fixed candle deduplication so forming updates and closed events have distinct identities.
- Scoped connector timestamp ordering to symbol/event type.
- Separated malformed-message handling from logged callback failures.
- Fixed missing symbol argument in tick portfolio risk checks.
- Added tests/test_feed_regressions.py; full suite: 77 passed.
- Added requirements-models.txt and scripts/download_models.py.
- Downloaded official amazon/chronos-2 to data/pretrained/chronos-2.
- Revision: 29ec3766d36d6f73f0696f85560a422f50e8498c.
- model.safetensors: 477930472 bytes; SHA256 ddcda3c7508bf2528087723e98a20707cc04b7f370ae275a9fd88078ddba4f42.
- Checksums in data/pretrained/chronos-2/download_manifest.json.

## In Progress / Not Completed

- Round 3 resolved pip TLS by verified PyPI wheel bootstrap (scripts/bootstrap_pip.py), upgrading pip 22.2.2 to 26.2.1. Installed LightGBM 4.7.0, CatBoost 1.2.10, chronos-forecasting 2.3.2. Transformers 5.17 incompatible with existing torch 2.4.1+cu121; pinned working Transformers 4.51.3 and huggingface-hub 0.36.2. pip check passes. Existing torch preserved; GPU is RTX 4070 Laptop. Chronos local-only CPU and CUDA real inference both finite, shape [1,21,4]. GPU warns no flash attention but executes.
- Two background installation/training subagents failed without results; no changes from them were found. Parent is taking over.
- Baseline now uses app/dataset_split.py time-grouped splits, label purging, mandatory quality validation and train-only normalization. Dataset generator records close-time availability and label_end_time, sorts by timestamp/symbol. Legacy datasets without label_end_time remain accepted with label_intervals_verified=false; production promotion must reject unverified intervals. Advanced trainer also uses this splitter as of round 2.
- Round 2: advanced gbst-v3 stores base_score and predicts gross_return; long and short costs deducted after position selection; sub-cost forecasts stay flat. Missing-base legacy artifacts explicitly require retraining. Signal calibration uses signed gross returns and never flips FLAT/SHORT incorrectly. New parity/cost regression tests pass. Full suite rerun: 82 passed; one prior run exposed a timing-sensitive 20ms persistence test, still needs deterministic synchronization.
- Walk-forward now reserves validation within each training window, partitions by timestamp groups and does not write model artifacts. Regression tests assert every window fits successfully. Full suite now 81 passed.
- Round 5: BatchWriter task_done occurs only after successful writes; drain awaits commit or raises worker failure; flush uses fixed batch deadline. Timing-sensitive test replaced with drain synchronization. app/database_worker.py provides single-thread-owned SQLite connection; main event/candle batch writers now use it with aiohttp cleanup hook. Full suite 88 passed. Account/strategy/order synchronous persistence, Round 11 tracked main tasks and added runner finally cleanup; test_runtime_shutdown starts mocked main with isolated data and verifies cancellation leaves no tasks. 97 passed with six preexisting-style aiohttp string-key warnings. Early initialization failures before runner setup still need review; price-source consistency still need work. Round 7: account accepts same-order partial fills with weighted entry and accumulated fees; broker.process optional accept_fill gates order confirmation, timestamp injectable; main both depth and process branches accept into current session account before confirmation, no terminal depth refill, rounded partial depth status correct. tests/test_partial_fill_account.py covers acceptance/rejection. 91 tests passed before final depth guard changes; compilation passed after. Crash-atomic order/account persistence and unique fill-id dedup still outstanding.
- app/chronos_model.py local-only forecast adapter and CLI implemented; real CPU prediction through adapter verified. Round 6 added app/shadow.py bounded per-symbol coalescing thread worker, main submits closed bars, /api/models reads shadow status. tests/test_shadow.py verifies deduplication and no production ownership. Real BTC database bars successfully predicted by LightGBM, CatBoost and Chronos with empty load_errors. Full suite 89 passed. Live service restart/API/browser verification still pending; shadow currently selects latest local candidate once at load, lifecycle refresh and persistent prediction audit remain pending. Round 4 added app/tabular_model.py native LightGBM/CatBoost training, validated label intervals, correct costs, checksum verified native loading, CLI, and tests/test_tabular_model.py. Actual BTC/ETH dataset generated independently at data/research_v3/training_dataset.jsonl (3788 rows, 60 purged, 570 test rows). Candidates: data/research_v3/candidates/lightgbm-1f552e1a62aa4d30be91c26574e27303 and data/research_v3/candidates/catboost-6fa1f6d179c34797ad3c1722e3b25e54. Both have zero active test samples above 12bp; no profitability claim or promotion. Full suite 86 passed. app/train.py now validates OHLCV continuity and accepts output path. Requirements pinned to tested model package versions.
- Round 8: WS feed respects configured interval and set_symbols closes/reconnects on actual symbol changes; main subscribes active symbols plus held/open-order symbols. Removed mixed mark/book/trade 1m synthetic candles; closed WS bars update memory directly; REST strategy consumes fetched rows rather than stale queued DB reads. tests/test_subscriptions.py added; full suite 93 passed. Round 12 removed fast_broadcast duplicate main tasks; periodic pump is sole automated broadcaster. Web serializes once, skips overlapping broadcasts, concurrently sends with 250ms timeout, drops/closes slow sockets. tests/test_broadcast.py verifies fast client receives despite blocked peer, no overlap duplicates. 98 passed, 7 aiohttp string-key warnings. Round 13 registry promotion requires verified label intervals, features-v1, finite accuracy, checksum, mandatory expiration (default 6h), cost-included portfolio_oos >=30 trades, positive net return and <=20% drawdown. These are supplied report gates, not independent backtest verification; report provenance remains pending. tests/test_promotion_safety.py rejects accuracy-only/unverified promotion. 100 tests passed, 7 warnings. Unified runtime/model lifecycle and service end-to-end verification not completed.
- Round 9: app/price_state.py validates per-role timestamped trade/book/mark fields. Main account.mark only called from WS mark_price; REST tick/last-bar/cached market snapshots no longer drive liquidation. REST uses request-start timestamp for arbitration. Strategy display preserves quotes and uses closed-bar time for guard. tests/test_price_state.py added; 95 passed. Round 10 adds execution_ready: recent (5s) quote+mark, positive finite prices, no crossed quotes or future timestamps. Tick and bar entries gated; settle_order overrides historical strategy price with latest quote and requires positive available size. REST premiumIndex mark fallback uses exchange time, rejects stale/future/regressing marks. 96 tests passed. Main market display arbitration, shared liquidity consumption, executable stop/target exits and lifecycle cleanup still pending. REST fallback not yet verified live. No service restart or live trading performed.

- Round 14: every simulated decision now runs through the real trained artifacts. New app/live_models.py owns one RealModelRuntime (promoted production model from data/models when the registry gates pass, otherwise the newest real LightGBM/CatBoost candidates under data/research_v3/candidates, plus an optional Chronos-2 quantile vote) and a ModelDecision service that emits the signal/plan shape the paper loop consumes. app/main.py tick strategy and closed-bar strategy both call decisions.signal()/decisions.plan(); app/shadow.py reuses the same runtime so weights load once. Inference runs in dedicated thread pools (tabular 2 workers, Chronos 1) with per-(symbol, bar) caching and cache invalidation when a Chronos vote lands. Signals record source, model_mode, model_version, per-member votes, edge_bps and expected_net_return into strategy_decision events, /api/state, and the model centre page. MODEL_RULE_FALLBACK defaults to 0, so an unavailable model blocks trading instead of silently trading rules. app/backtest.py gained a strategy hook and a --models flag (python -m app.backtest FILE --models) that runs the identical ModelDecision math offline. Round 14 follow-up: the decision cache key and every Chronos cache/vote/invalidate call now share one bar identity (close_time, falling back to open_time), so the Chronos vote actually joins the ensemble instead of being recomputed for a mismatching id; verified live (chronos forecasts are produced and reused once per bar). Added a training feature-space guard: the runtime recomputes the sha256 of data/research_v3/training_dataset.jsonl, verifies it against the candidate manifest dataset_sha256, derives per-feature min/max, and flags out-of-range inputs. This matters because the current weights are BTC/ETH-trained while ema20/ema50/atr carry absolute price levels, so alt symbols are out-of-distribution. MODEL_OOD_POLICY=block (default) refuses those directions, warn only annotates; the offending feature names ride along in the decision event, /api/state and the model centre. tests/test_live_models.py adds 13 cases (ensemble voting, agreement/edge floors, Chronos vote, fail-closed, plan geometry, per-bar cache, real candidate artifact loading, shared shadow runtime, no rule path in main). Full suite: 111 passed. Candidate weights are still unpromoted and their manifests report zero active samples above 12bp, which the UI and docs state explicitly; no profitability claim is made.

## Continuation Verification

- Added the trained-symbol session source and selector. Only symbols from a dataset matching every loaded artifact hash are offered; existing sessions remain unchanged.
- Full suite: 116 passed, 7 existing aiohttp AppKey warnings. The new selector/backend changes require restarting the existing paper process; this continuation did not restart it or replace the running session.

## Next Steps

Retrain and promote a production model that actually clears costs (MODEL_MIN_EDGE_BPS=12 currently rejects every candidate), then verify a live paper-service restart against the real exchange feed. Diagnose pip network transport without exposing proxy credentials; complete remaining implementation with focused regression tests. Read existing files before modifications. Preserve existing training artifacts. No production model should be promoted based on synthetic smoke tests.
